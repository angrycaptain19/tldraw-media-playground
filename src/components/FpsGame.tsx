// ─── FpsGame barrel re-export ─────────────────────────────────────────────────
// Matches the PlatformerGame.tsx / DuckHuntGame.tsx pattern — a single default
// re-export so consumers can import from 'components/FpsGame' directly.
export { default } from './fps/FpsGame'
